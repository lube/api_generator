 /** 
{% if Entity['Con-Rol'] %}   * @PreAuthorize("hasRole('{{ Entity['Rol'] }}')")
{% endif %}
   * @Route("/{id}", requirements={"id" = "\d+"})
   * @Method({"POST"})
   * @ParamConverter("{{ Entity['Name'] | lower }}", class="{{ Bundle['Name'] }}:{{ Entity['Name'] }}")
   * @ApiDoc(
   *  description="Edita una {{ Entity['Name'] }}"
   * )
   */
  public function updateAction(Request $request, ${{ Entity['Name'] | lower }})
  {
    $contex = new \JMS\Serializer\DeserializationContext();
    $context->attributes->set('target', ${{ Entity['Name'] | lower }});

    $this->serializer->deserialize($request->request, '{{ $Entity['Metadata'].getName() }}', 'json', $context);
    
    $errors = $this->get('validator')->validate($cajero);
    if (count($errors) > 0) 
    {
        foreach ($errors->getIterator() as $error) 
        {
            $errorMessages[] = $error->getMessage();
        }

        return new JsonResponse($errorResponse, 400);
    }

    $this->em->persist(${{EntityName | lower}});
    $this->em->flush();

    return new JsonResponse(json_decode($this->serializer->serialize(${{ Entity['Name'] | lower }}, 'json')));
  }